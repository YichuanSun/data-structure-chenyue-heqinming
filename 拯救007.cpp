#include <bits/stdc++.h>
#define SIZE 10005
using namespace std;
typedef struct point{
	int x,y;
}point;
point coord[SIZE];
double dis(point a,point b)	{return sqrt((a.x-b.x)*(a.x-b.x)+(a.y-b.y)*(a.y-b.y));}
int main()	{
	int n,len;
	for (int i=0;i<n;i++)	scanf("%d %d",&coord[i].x,&coord[i].y);
	for (int i=0;i<n;i++)	{
		for ()
	}
	return 0;
}
